// place files you want to import through the `$lib` alias in this folder.
import type { MovieResponseType, MovieVideosResultsType } from '$lib/types';
import { APIUrls } from '$lib/APIUrls';

export type { MovieResponseType, MovieVideosResultsType };

export { APIUrls };
